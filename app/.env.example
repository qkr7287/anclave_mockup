# 복사해서 .env 로 쓰고 값 채우기:  cp .env.example .env
# Hono backend(REST) 베이스 URL. 미설정 시 http://localhost:8787 로 폴백(src/lib/api.ts).
#   로컬 backend:  http://localhost:8787
#   63번 backend:  http://192.168.0.63:8787
VITE_API_BASE=http://localhost:8787
